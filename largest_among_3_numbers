#include <stdio.h>

int main()
{
    int a;
    printf("enter a number");
    scanf("%d",&a);
    if(a%2==0)
    {
        printf("it is an even number.");
    }
    if(a%2!=0)
    {
        printf("it is an odd number");
    }
    if(a==0)
    {
        printf("Zero");
    }
    return 0;
}
