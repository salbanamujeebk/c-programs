#include <stdio.h>

int main()
{
    int arr[10], i,sum = 0;
    printf("Enter the elements:  ");
    for (i = 0; i < 10; i++) {
        scanf("%d", &arr[i]);
        sum += arr[i];
    }

    printf("The sum of elements in the array : %d\n", sum);

    return 0;
}
